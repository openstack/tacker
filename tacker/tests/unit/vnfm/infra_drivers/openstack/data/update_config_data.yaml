vdus:
  vdu1:
    config:
      firewall: |
        package firewall

        config defaults
          option syn_flood '3'
          option input 'ACCEPT'
          option output 'ACCEPT'
          option forward 'ACCEPT'
